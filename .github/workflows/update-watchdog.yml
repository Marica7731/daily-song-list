name: Watch published song-list freshness

on:
  schedule:
    - cron: "37 * * * *"
  workflow_dispatch:

permissions:
  actions: write
  contents: read

concurrency:
  group: daily-song-list-watchdog
  cancel-in-progress: true

jobs:
  watchdog:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Check published freshness
        id: watchdog
        env:
          GH_TOKEN: ${{ github.token }}
        run: node scripts/watchdog-update.js

      - name: Summary
        if: always()
        run: |
          {
            echo "### Watchdog"
            echo "- triggered: ${{ steps.watchdog.outputs.triggered }}"
            echo "- reason: ${{ steps.watchdog.outputs.reason }}"
            echo "- trigger_reason: ${{ steps.watchdog.outputs.triggerReason }}"
            echo "- source_run_id: ${{ steps.watchdog.outputs.sourceRunId }}"
            echo "- captured_at: ${{ steps.watchdog.outputs.captured_at }}"
            echo "- previous_captured_at: ${{ steps.watchdog.outputs.previousCapturedAt }}"
            echo "- age_minutes: ${{ steps.watchdog.outputs.age_minutes }}"
            echo "- dispatched_run_id: ${{ steps.watchdog.outputs.dispatchedRunId }}"
          } >> "$GITHUB_STEP_SUMMARY"
